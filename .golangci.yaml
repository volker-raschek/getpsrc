version: "2"
linters:
  default: standard
  enable:
  - errname
  - gosec

  exclusions:
    rules: []
    warn-unused: true

  settings:
    gosec:
      excludes:
        - G706

run:
  tests: true
